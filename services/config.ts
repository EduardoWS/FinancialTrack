// Configuração para controlar fonte de dados
export const APP_CONFIG = {
  USE_API: true, // Trocar para true quando a API estiver pronta
  API_BASE_URL: 'https://sua-api.com', // URL da API quando estiver pronta
  MOCK_DELAY: 1000, // Delay para simular chamadas de API (em ms)
}; 